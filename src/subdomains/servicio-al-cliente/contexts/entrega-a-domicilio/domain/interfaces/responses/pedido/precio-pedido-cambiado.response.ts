
/**
 *Interfaz para la respuesta a Precio Pedido cambiado
 *
 * @export
 * @interface IPrecioPedidoCambiadoResponse
 */
 export interface IPrecioPedidoCambiadoResponse {
    success: boolean;
    data: number | null;
}